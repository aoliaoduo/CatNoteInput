<Window x:Class="CatNoteInput.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CatNoteInput"
        mc:Ignorable="d"
        Title="CatNote API Input"
        Height="760" Width="1060"
        MinHeight="680" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Icon="Assets/CatNoteInput.ico"
        Background="{StaticResource AppBackgroundBrush}"
        FontFamily="Segoe UI Variable, Segoe UI"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <local:StringNullOrEmptyToVisibilityConverter x:Key="StringNullOrEmptyToVisibilityConverter" />
        <local:BooleanNegationConverter x:Key="BooleanNegationConverter" />
    </Window.Resources>
    <Window.InputBindings>
        <KeyBinding Key="Enter" Modifiers="Control" Command="{Binding SendCommand}" />
    </Window.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="24,24,24,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Width="52"
                        Height="52"
                        CornerRadius="12"
                        Background="{StaticResource AccentBrush}">
                    <Image Source="Assets/CatNoteInput.ico"
                           Width="26"
                           Height="26"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center" />
                </Border>
                <StackPanel Grid.Column="1" Margin="16,0,0,0">
                    <TextBlock Text="CatNote API 输入"
                               FontSize="26"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource PrimaryTextBrush}" />
                    <TextBlock Text="快速录入 CatNote 笔记，一键发送，自动记住密钥"
                               FontSize="12"
                               Margin="0,6,0,0"
                               Foreground="{StaticResource SecondaryTextBrush}" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="24,0,24,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource SectionCardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="笔记内容"
                               FontSize="15"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource PrimaryTextBrush}" />
                    <TextBlock Grid.Row="1"
                               Text="支持长文本，最多 5000 字。"
                               FontSize="12"
                               Margin="0,4,0,0"
                               Foreground="{StaticResource MutedTextBrush}" />

                    <Border Grid.Row="2" Style="{StaticResource InputBorderStyle}" Margin="0,12,0,0">
                        <Grid>
                            <TextBox Style="{StaticResource InputTextBoxStyle}"
                                     Text="{Binding ContentText, UpdateSourceTrigger=PropertyChanged}"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"
                                     Height="240"
                                     IsEnabled="{Binding IsBusy, Converter={StaticResource BooleanNegationConverter}}" />
                            <TextBlock Text="输入笔记内容…"
                                       Foreground="{StaticResource MutedTextBrush}"
                                       Margin="12,10,12,0"
                                       IsHitTestVisible="False"
                                       Visibility="{Binding ContentText, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}" />
                        </Grid>
                    </Border>

                    <Grid Grid.Row="3" Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="提示: Ctrl+Enter 发送"
                                   Foreground="{StaticResource SecondaryTextBrush}"
                                   VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1"
                                   Text="{Binding ContentLengthDisplay}"
                                   Foreground="{Binding ContentLengthBrush}"
                                   VerticalAlignment="Center" />
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource SectionCardStyle}" Margin="0,16,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="专属 API"
                               FontSize="15"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource PrimaryTextBrush}" />
                    <TextBlock Grid.Row="1"
                               Text="输入 yoursecret 或完整地址，自动识别。"
                               FontSize="12"
                               Margin="0,4,0,0"
                               Foreground="{StaticResource MutedTextBrush}" />

                    <Border Grid.Row="2" Style="{StaticResource InputBorderStyle}" Margin="0,12,0,0">
                        <Grid>
                            <TextBox Style="{StaticResource InputTextBoxStyle}"
                                     Text="{Binding ApiSecret, UpdateSourceTrigger=PropertyChanged}"
                                     IsEnabled="{Binding IsBusy, Converter={StaticResource BooleanNegationConverter}}" />
                            <TextBlock Text="输入密钥或完整 API 地址"
                                       Foreground="{StaticResource MutedTextBrush}"
                                       Margin="12,8,12,0"
                                       IsHitTestVisible="False"
                                       Visibility="{Binding ApiSecret, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}" />
                        </Grid>
                    </Border>

                    <CheckBox Grid.Row="3"
                              Content="记住密钥"
                              Margin="2,12,0,0"
                              IsChecked="{Binding RememberSecret}"
                              Foreground="{StaticResource SecondaryTextBrush}" />
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2"
                Margin="24,16,24,24"
                CornerRadius="10"
                BorderThickness="1"
                BorderBrush="{StaticResource CardBorderBrush}"
                Background="{StaticResource CardSurfaceBrush}"
                Padding="14,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ProgressBar Grid.Column="0"
                             Width="120"
                             Height="4"
                             IsIndeterminate="{Binding IsBusy}"
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                             Foreground="{StaticResource AccentBrush}"
                             Background="{StaticResource SubtleLineBrush}"
                             Margin="0,0,12,0" />
                <TextBlock Grid.Column="1"
                           Text="{Binding StatusMessage}"
                           Foreground="{Binding StatusBrush}"
                           FontSize="12"
                           VerticalAlignment="Center" />
                <Button Grid.Column="2"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding SendCommand}"
                        Content="{Binding SendButtonText}"
                        MinWidth="140" />
            </Grid>
        </Border>
    </Grid>
</Window>
